base origin/pu

merge unstuck-diff-algorithm

  Sent upstream.

merge merge-doc-diff-algorithm

  Sent upstream.

merge submodule-diff-graph

  Sent upstream.

merge completion-fixes

merge doc-makefile-fix

merge merge-tree-same-added-in-sub

  Currently in pu but should be re-rolled if someone responds to this
  message:

    http://article.gmane.org/gmane.comp.version-control.git/219378

merge rebase-i-comment-squash

  Comment out the first line of a "!squash" commit when generating a
  commit message in git-rebase.

  Merged to jk/pu on 2013-03-30.  Dogfooding for a while to see if
  this is a sensible change.

merge reset-submodule-trailing-slash

  Waiting for nd/magic-pathspecs to graduate to master.
